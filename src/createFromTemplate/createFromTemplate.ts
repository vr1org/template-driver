import { TDContext } from "../context/TDContext";

export async function createFromTemplate(target: string, template: string, context: TDContext): Promise<void> {
  // const version = await getTemplateInfo(template, options);
  // greenPrint(`copying files from ${template}, for package ${target}`);
  // await copyReadmeFile(template.templateName, params);
  // await copyPackageJson(template.templateName, params);
  // await copyFiles(template.templateName, params);
  // greenPrint(`copying files finished`);
  // await npmInstall(params);
  // await writeScaffoldJson(template, params);
}
